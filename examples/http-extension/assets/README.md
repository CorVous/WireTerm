# Local assets

Place optional relative PNG/JPEG files here and resolve them with
`wireterm.asset("assets/name.png")`. The shipped HTTP example is vector/text
only so its automated acceptance fixture remains minimal.
